# ============================================================================
# Environment & PATH Configuration
# ============================================================================

# fnm (Fast Node Manager) setup
export PATH=/home/$USER/.fnm:$PATH
eval "$(fnm env --use-on-cd --version-file-strategy=recursive)"

# pnpm (Package Manager) setup
export PNPM_HOME="/home/$USER/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac

# Preferred editor for local and remote sessions
export EDITOR=nvim

# Compilation flags
# export ARCHFLAGS="-arch x86_64"

# ============================================================================
# Oh My Zsh Configuration
# ============================================================================

export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME="robbyrussell"
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

plugins=(
  git
  fzf-tab
  zsh-syntax-highlighting
  pnpm
  archlinux
)

source $ZSH/oh-my-zsh.sh

# ============================================================================
# Plugin Initialization
# ============================================================================

source ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh
bindkey ';;'  autosuggest-accept

# ============================================================================
# History & Completion Settings
# ============================================================================

HISTSIZE=5000
HISTFILE="~/.zsh_history"
HISTDUP="erase"
setopt appendhistory sharehistory hist_ignore_space hist_ignore_all_dups hist_save_no_dups hist_ignore_dups hist_find_no_dups

zstyle ':completions:*' matcher-list 'm:{a-z}={A-Za-z}'
zstyle ':completions:*' list-colors '${(s,:.)LS_COLORS}'
zstyle ':completions:*' menuprompt='%F{yellow}%B%n@%m%f:%B%d %B%p%fB:'
zstyle ':fzf-tab:complete:cd:*' fzf-preview 'ls --color $realpath'

# Completion behavior
# CASE_SENSITIVE="true"
# HYPHEN_INSENSITIVE="true"
# ENABLE_CORRECTION="true"
# COMPLETION_WAITING_DOTS="true"

# OMZ update behavior
# zstyle ':omz:update' mode disabled
# zstyle ':omz:update' mode auto
# zstyle ':omz:update' mode reminder
# zstyle ':omz:update' frequency 13

# ============================================================================
# External Tool Initialization
# ============================================================================

export FZF_DEFAULT_OPTS='--color=fg:#f8f8f2,bg:#282a36,hl:#bd93f9 --color=fg+:#f8f8f2,bg+:#44475a,hl+:#bd93f9 --color=info:#ffb86c,prompt:#50fa7b,pointer:#ff79c6 --color=marker:#ff79c6,spinner:#ffb86c,header:#6272a4'
source /usr/share/fzf/completion.zsh
source /usr/share/fzf/key-bindings.zsh

eval "$(starship init zsh)"
eval "$(zoxide init zsh)"
eval "$(atuin init zsh)"
# . "$HOME/.atuin/bin/env"

if command -v wt >/dev/null 2>&1; then eval "$(command wt config shell init zsh)"; fi

# ============================================================================
# Aliases - General
# ============================================================================

alias zshconfig="nvim ~/.zshrc"
alias nvconfig="cd ~/.config/nvim/lua/; nvim plugins/init.lua"
alias i3config="nvim ~/.config/i3/config"
alias termconfig="nvim ~/.wezterm.lua"
alias t="tig"
alias ts="tig status"
alias ls="ls --color"
alias nvimdiff="nvim -d"
alias n="nvim"
alias v="nvim"
alias lz="lazydocker"
alias batm="bat --language=markdown"
alias p='wl-paste'

# ============================================================================
# Aliases - Docker
# ============================================================================

alias dup="sudo systemctl start docker.service; docker-compose up"
alias dls='function _dgrep() { echo "Docker containers"; docker ps -a | grep "$1"; echo "Docker images"; docker images | grep "$1" }; _dgrep'
alias ddown="sudo systemctl stop docker.socket; sudo systemctl stop docker.service"

# ============================================================================
# Aliases - AI/LLM
# ============================================================================

alias "aif"="aichat -r %functions%"
alias "aic"="aichat -c"
alias "x"="aichat -e"
alias ","="aichat"
alias fabric="fabric-ai --disable-responses-api --stream"

# ============================================================================
# Aliases - AI Models
# ============================================================================

export OPENCODE_ENABLE_EXA=1 

alias "ai-qwen3.5-thinking"='toolbox run --container llama-vulkan-amdvlk -- llama-server \
  --port 8080 \
  -m ~/.cache/llama.cpp/muddler_Qwen3.5-35B-A3B-APEX-I-Quality.gguf \
  --mmproj ~/.cache/llama.cpp/muddler_Qwen3.5-35B-A3B-APEX-mmproj-F16.gguf \
  --alias QWEN_SMALL \
  -ngl 999 \
  --no-mmap \
  --flash-attn on --fit on \
  --ctx-size 202752 \
  --ctx-checkpoints 8192 \
  --cache-type-k q8_0 --cache-type-v q8_0 \
  --flash-attn on --fit on \
  --kv-unified \
  --keep -1 \
  -np 1 \
  --jinja \
  --temp 0.6 \
  --top-p 0.95 \
  --top-k 20 \
  --min-p 0.05 \
  --repeat-penalty 1.0 \
  --presence-penalty 1.4 \
  --checkpoint-every-n-tokens 2048 \
  --chat-template-kwargs "{\"enable_thinking\": true}"'

alias "ai-minimax-M2.5-thinking"='toolbox run --container llama-vulkan-amdvlk -- llama-server \
  --port 8080 \
  -m ~/.cache/llama.cpp/unsloth_MiniMax-M2.5-GGUF_UD-Q2_K_XL_MiniMax-M2.5-UD-Q2_K_XL-00001-of-00003.gguf \
  --alias MINIMAX \
  --host 0.0.0.0 \
  -ngl 999 \
  --no-mmap \
  -fa 1 \
  --ctx-size 65536 \
  --ctx-checkpoints 8192 \
  --cache-type-k f16 \
  --cache-type-v f16 \
  --jinja \
  --swa-full \
  -np 1 \
  --temp 1 \
  --top-p 0.95 \
  --top-k 40 \
  --min-p 0.00 \
  --repeat-penalty 1.0 \
  --checkpoint-every-n-tokens 2048 \
  --chat-template-kwargs "{\"enable_thinking\": true}"'

# ============================================================================
# Custom Functions
# ============================================================================

alias-fzf() {
  local selected_alias=$(alias | fzf | awk -F'=' '{print $1}')
  if [[ -n $selected_alias ]]; then
    eval $selected_alias
  fi
}
zle -N alias-fzf
bindkey '^A' alias-fzf

_aichat_zsh() {
    if [[ -n "$BUFFER" ]]; then
        local _old=$BUFFER
        BUFFER+="⌛"
        zle -I && zle redisplay
        BUFFER=$(aichat -e "$_old")
        zle end-of-line
    fi
}
zle -N _aichat_zsh
bindkey '\ee' _aichat_zsh

# ============================================================================
# Additional Setup
# ============================================================================

export PATH="/home/pico/.local/bin:$PATH"
source ~/.profile
. "$HOME/.local/share/../bin/env"
